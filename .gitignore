# Dependencies
node_modules/

# Next.js
.next/
out/

# Secrets — NEVER commit
.env.local
.env*.local

# Sentry build plugin (source-map upload auth token)
.env.sentry-build-plugin

# Build
dist/

# Local disk cache (news, etc.)
.cache/

# OS
.DS_Store
.pm2/

# AI engine config (contains API keys)
.ai-config.json

# Python
.mlx_venv/
__pycache__/
*.pyc

# World vault — now stored in Supabase vault_notes, not filesystem
world-vault/
